node_modules

# Output
.output
.vercel
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Must be `.idea/*` rather than a bare `.idea`: ignoring the directory itself
# makes Git skip everything inside it, so the negations below can't re-add the
# shared editor config.
.idea/*
!.idea/prettier.xml
!.idea/jsLinters/
TODO
